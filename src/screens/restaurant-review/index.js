import React from 'react';
import PostReview from '../../components/post-review';

const RestaurantReview = ({navigation, route}) => {
  return <PostReview navigation={navigation} route={route} />;
};
export default RestaurantReview;
